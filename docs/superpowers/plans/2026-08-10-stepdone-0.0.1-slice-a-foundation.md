# StepDone 0.0.1 Slice A — Foundation & API Spine Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建精简 monorepo、MySQL/Redis、模板状态机、mock 鉴权、假 Agent Worker、SSE，以及到 mock 支付解锁为止的服务端主链路 API（不含完整 UI）。

**Architecture:** pnpm workspace；`apps/web` 作 BFF；`apps/agent-worker` 消费 BullMQ 并内嵌 Outbox 投递；MySQL 为事实源；fixture 节点通过 Zod 校验后落库；支付与权益在服务端事务中完成。

**Tech Stack:** Next.js 16.3 App Router、React 19、TypeScript、Prisma、MySQL 8、Redis、BullMQ、Zod、Vitest、pnpm、Turbo。

**Spec:** `docs/superpowers/specs/2026-08-10-stepdone-0.0.1-vertical-slice-design.md`  
**Companion plan:** `docs/superpowers/plans/2026-08-10-stepdone-0.0.1-slice-b-ui-e2e.md`（UI + Playwright）

## Global Constraints

- 产品名文案：逐成 AI / StepDone AI；口号「一步一步，把事情做成。」
- Next.js **16.3.0**（改路由/API 前先读 `node_modules/next/dist/docs/`）
- 金额用**整数分**；对外 ID 用 **ULID**（`publicId`）；时间 **UTC**
- 队列消息只传 ID 引用，不传完整 Prompt/文件
- `MOCK_PAYMENTS=1`（或非 production）才允许 mock 支付确认
- 禁止实现：小程序、真微信/短信/模型/搜索、真导出引擎
- 包管理：**pnpm**；测试框架 packages 用 **Vitest**
- 每个 Task 结束必须 commit

### File map（本计划锁定）

| 路径 | 职责 |
|---|---|
| `pnpm-workspace.yaml`, `turbo.json`, 根 `package.json` | workspace 脚本 |
| `infrastructure/docker/docker-compose.yml` | MySQL 8 + Redis |
| `packages/domain/` | 枚举与错误码 |
| `packages/database/` | Prisma schema、client、seed |
| `packages/schemas/` | Zod DTO / 节点输出 |
| `packages/config/` | env 解析 |
| `packages/auth/` | session 签发与校验 |
| `packages/project-engine/` | 模板 v1 + 状态转换 |
| `packages/agent-core/` | 租约、幂等键、Node 结果类型 |
| `packages/payments/` | 下单与 mock 确认事务 |
| `apps/web/` | Next app + Route Handlers + SSE |
| `apps/agent-worker/` | Outbox loop + BullMQ consumer + fixtures |
| `packages/*/src/**/*.test.ts` | 单元测试 |

---

### Task 1: Monorepo scaffold + Docker

**Files:**
- Create: `pnpm-workspace.yaml`
- Create: `turbo.json`
- Create: `infrastructure/docker/docker-compose.yml`
- Create: `.env.example`
- Modify: 根 `package.json`
- Move: 现有 Next 应用 → `apps/web/`（`app/`, `public/`, `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `eslint.config.mjs`, `next-env.d.ts`）

**Interfaces:**
- Produces: `pnpm dev` 可同时文档化启动 web（本 task 先保证 `apps/web` 可 `pnpm --filter @stepdone/web dev`）；Compose 服务名 `mysql`/`redis`

- [ ] **Step 1: 写 workspace 与 compose 文件**

`pnpm-workspace.yaml`:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

`infrastructure/docker/docker-compose.yml`:
```yaml
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: stepdone
      MYSQL_USER: stepdone
      MYSQL_PASSWORD: stepdone
    ports: ["3306:3306"]
    command: ["--default-authentication-plugin=mysql_native_password", "--character-set-server=utf8mb4"]
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
```

`.env.example`:
```bash
APP_ENV=local
APP_URL=http://localhost:3000
DATABASE_URL=mysql://stepdone:stepdone@127.0.0.1:3306/stepdone
REDIS_URL=redis://127.0.0.1:6379
SESSION_SECRET=dev-only-change-me-32chars-minimum!!
MOCK_PAYMENTS=1
FIELD_ENCRYPTION_KEY=dev-only-32-byte-key-base64-or-hex!!
```

根 `package.json` scripts（保留 private name `stepdone`）:
```json
{
  "name": "stepdone",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "db:migrate": "pnpm --filter @stepdone/database exec prisma migrate dev",
    "db:seed": "pnpm --filter @stepdone/database exec prisma db seed",
    "docker:up": "docker compose -f infrastructure/docker/docker-compose.yml up -d"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  }
}
```

- [ ] **Step 2: 迁移 Next 应用到 `apps/web`**

```bash
mkdir -p apps/web
git mv app public next.config.ts tsconfig.json postcss.config.mjs eslint.config.mjs next-env.d.ts apps/web/ 2>/dev/null || mv app public next.config.ts tsconfig.json postcss.config.mjs eslint.config.mjs next-env.d.ts apps/web/
```

创建 `apps/web/package.json`:
```json
{
  "name": "@stepdone/web",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3000",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "next": "16.3.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.0",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^3.0.0"
  }
}
```

删除根目录旧 `package-lock.json`；启用 pnpm：
```bash
corepack enable
pnpm install
pnpm docker:up
pnpm --filter @stepdone/web build
```

Expected: Docker healthy；`apps/web` build 成功。

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: scaffold pnpm monorepo and docker compose"
```

---

### Task 2: `packages/domain` 枚举与错误码

**Files:**
- Create: `packages/domain/package.json`
- Create: `packages/domain/tsconfig.json`
- Create: `packages/domain/src/index.ts`
- Create: `packages/domain/src/status.ts`
- Create: `packages/domain/src/errors.ts`
- Test: `packages/domain/src/status.test.ts`

**Interfaces:**
- Produces: `ProjectStatus`, `StepStatus`, `ErrorCode`, `NodeCode` 字面量联合类型与常量数组

- [ ] **Step 1: 写失败测试**

```typescript
// packages/domain/src/status.test.ts
import { describe, it, expect } from "vitest";
import { PROJECT_STATUSES, canTransitionProject } from "./status";

describe("canTransitionProject", () => {
  it("allows ACTIVE -> PAYMENT_REQUIRED", () => {
    expect(canTransitionProject("ACTIVE", "PAYMENT_REQUIRED")).toBe(true);
  });
  it("rejects COMPLETED -> ACTIVE", () => {
    expect(canTransitionProject("COMPLETED", "ACTIVE")).toBe(false);
  });
});
```

- [ ] **Step 2: 运行确认失败**

```bash
pnpm --filter @stepdone/domain test
```
Expected: FAIL（包或模块不存在）

- [ ] **Step 3: 实现**

`packages/domain/package.json`:
```json
{
  "name": "@stepdone/domain",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "test": "vitest run", "lint": "echo ok" },
  "devDependencies": { "vitest": "^3.0.0", "typescript": "^5.8.0" }
}
```

`packages/domain/src/status.ts`:
```typescript
export const PROJECT_STATUSES = [
  "DRAFT", "ACTIVE", "WAITING_USER", "AI_PROCESSING",
  "PAYMENT_REQUIRED", "QUALITY_REVIEW", "COMPLETED",
  "ARCHIVED", "FAILED", "CANCELLED",
] as const;
export type ProjectStatus = (typeof PROJECT_STATUSES)[number];

export const STEP_STATUSES = [
  "PENDING", "QUEUED", "RUNNING", "WAITING_USER", "VALIDATING",
  "SUCCEEDED", "FAILED_RETRYABLE", "FAILED_FINAL", "CANCELLED",
] as const;
export type StepStatus = (typeof STEP_STATUSES)[number];

export const NODE_CODES = [
  "DEFINE_OBJECTIVE", "CREATE_PLAN", "SELECT_COMPETITORS", "RESEARCH_SOURCES",
  "SELECT_DIMENSIONS", "BUILD_MATRIX", "USER_JUDGMENT", "GENERATE_PREVIEW",
  "PAYMENT_GATE", "GENERATE_REPORT", "QUALITY_REVIEW", "FINAL_CONFIRMATION",
  "EXPORT", "ABILITY_REPORT",
] as const;
export type NodeCode = (typeof NODE_CODES)[number];

const ALLOWED: Record<ProjectStatus, ProjectStatus[]> = {
  DRAFT: ["ACTIVE", "CANCELLED"],
  ACTIVE: ["WAITING_USER", "AI_PROCESSING", "PAYMENT_REQUIRED", "QUALITY_REVIEW", "COMPLETED", "FAILED", "CANCELLED", "ARCHIVED"],
  WAITING_USER: ["ACTIVE", "AI_PROCESSING", "FAILED", "CANCELLED"],
  AI_PROCESSING: ["ACTIVE", "WAITING_USER", "PAYMENT_REQUIRED", "FAILED", "CANCELLED"],
  PAYMENT_REQUIRED: ["ACTIVE", "CANCELLED", "ARCHIVED"],
  QUALITY_REVIEW: ["COMPLETED", "ACTIVE", "FAILED"],
  COMPLETED: ["ARCHIVED"],
  ARCHIVED: [],
  FAILED: ["ACTIVE", "CANCELLED", "ARCHIVED"],
  CANCELLED: ["ARCHIVED"],
};

export function canTransitionProject(from: ProjectStatus, to: ProjectStatus): boolean {
  return ALLOWED[from].includes(to);
}
```

`packages/domain/src/errors.ts`:
```typescript
export const ErrorCodes = {
  AUTH_REQUIRED: "AUTH_REQUIRED",
  PROJECT_NOT_FOUND: "PROJECT_NOT_FOUND",
  PROJECT_REVISION_CONFLICT: "PROJECT_REVISION_CONFLICT",
  STEP_INVALID_TRANSITION: "STEP_INVALID_TRANSITION",
  PAYMENT_NOT_ALLOWED: "PAYMENT_NOT_ALLOWED",
  ENTITLEMENT_REQUIRED: "ENTITLEMENT_REQUIRED",
  AGENT_NOT_RETRYABLE: "AGENT_NOT_RETRYABLE",
} as const;
export type ErrorCode = (typeof ErrorCodes)[keyof typeof ErrorCodes];
```

`packages/domain/src/index.ts` 导出上述符号。

- [ ] **Step 4: 测试通过并 commit**

```bash
pnpm --filter @stepdone/domain test
git add packages/domain
git commit -m "feat(domain): add project/step statuses and transition rules"
```

---

### Task 3: Prisma schema + migrate + seed

**Files:**
- Create: `packages/database/package.json`
- Create: `packages/database/prisma/schema.prisma`
- Create: `packages/database/prisma/seed.ts`
- Create: `packages/database/src/client.ts`
- Create: `packages/database/src/ulid.ts`

**Interfaces:**
- Produces: `prisma` singleton；`newPublicId()`；seed 写入 `competitor-analysis@1`、产品 `STANDARD_PROJECT`/`PRO_PROJECT`

- [ ] **Step 1: 定义 schema（核心模型）**

`packages/database/prisma/schema.prisma` 必须包含（字段对齐 spec）：
`User`, `UserIdentity`, `UserSession`, `ProjectTemplate`, `ProjectTemplateVersion`, `ProjectTemplateStep`, `Project`（含 `revision Int @default(0)`）, `ProjectStepRun`, `ProjectDecision`, `ProjectEvent`, `AgentRun`, `ModelUsage`, `OutboxEvent`, `Source`, `Citation`, `Artifact`, `ArtifactVersion`, `Export`, `Product`, `Order`, `PaymentTransaction`, `Entitlement`, `EntitlementTransaction`, `SkillDimension`, `SkillAssessment`。

关键片段：
```prisma
model Project {
  id                BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  publicId          String   @unique @map("public_id") @db.Char(26)
  userId            BigInt   @map("user_id") @db.UnsignedBigInt
  templateVersionId BigInt   @map("template_version_id") @db.UnsignedBigInt
  title             String   @db.VarChar(128)
  objective         String?  @db.Text
  currentStepCode   String?  @map("current_step_code") @db.VarChar(64)
  progress          Int      @default(0)
  status            String   @default("DRAFT") @db.VarChar(32)
  revision          Int      @default(0)
  metadata          Json?    @map("metadata_json")
  // timestamps + relations ...
  @@map("projects")
}

model AgentRun {
  id             BigInt  @id @default(autoincrement()) @db.UnsignedBigInt
  publicId       String  @unique @map("public_id") @db.Char(26)
  projectId      BigInt  @map("project_id") @db.UnsignedBigInt
  stepRunId      BigInt? @map("step_run_id") @db.UnsignedBigInt
  nodeCode       String  @map("node_code") @db.VarChar(64)
  idempotencyKey String  @unique @map("idempotency_key") @db.VarChar(191)
  status         String  @default("QUEUED") @db.VarChar(32)
  inputVersion   Int     @default(1) @map("input_version")
  // lease fields: lockedBy, lockExpiresAt, heartbeatAt
  @@map("agent_runs")
}

model OutboxEvent {
  id          BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  status      String    @default("PENDING") @db.VarChar(32)
  topic       String    @db.VarChar(64)
  payload     Json      @map("payload_json")
  availableAt DateTime  @default(now()) @map("available_at")
  attempts    Int       @default(0)
  publishedAt DateTime? @map("published_at")
  @@index([status, availableAt])
  @@map("outbox_events")
}
```

`datasource db { provider = "mysql" url = env("DATABASE_URL") }`  
`generator client { provider = "prisma-client-js" }`

- [ ] **Step 2: ULID 与 client**

```typescript
// packages/database/src/ulid.ts
import { ulid } from "ulid";
export function newPublicId(): string {
  return ulid();
}
```

```typescript
// packages/database/src/client.ts
import { PrismaClient } from "@prisma/client";
const globalForPrisma = globalThis as unknown as { prisma?: PrismaClient };
export const prisma = globalForPrisma.prisma ?? new PrismaClient();
if (process.env.APP_ENV !== "production") globalForPrisma.prisma = prisma;
```

- [ ] **Step 3: Seed 模板与产品**

`seed.ts` 创建：
- template `code=competitor-analysis` version `1`，steps 按 NODE_CODES 顺序写入 `requiresUserDecision` 标志（`CREATE_PLAN` 确认、`SELECT_COMPETITORS`、`SELECT_DIMENSIONS`、`USER_JUDGMENT` 为 true；`PAYMENT_GATE` 标记 `requiresPayment`）
- products: `STANDARD_PROJECT` priceFen=2900；`PRO_PROJECT` priceFen=5900
- skill_dimensions 六项：目标定义、信息检索、事实核查、比较分析、结构化表达、AI协作

- [ ] **Step 4: Migrate 并验证**

```bash
pnpm docker:up
pnpm --filter @stepdone/database exec prisma migrate dev --name init
pnpm db:seed
pnpm --filter @stepdone/database exec prisma db execute --stdin <<< "SELECT code FROM products;"
```
Expected: 两行产品 code。

- [ ] **Step 5: Commit**

```bash
git add packages/database
git commit -m "feat(database): add Prisma schema, migration, and seed data"
```

---

### Task 4: Zod schemas（目标表单 + 节点输出）

**Files:**
- Create: `packages/schemas/src/objective.ts`
- Create: `packages/schemas/src/nodes.ts`
- Create: `packages/schemas/src/api.ts`
- Test: `packages/schemas/src/objective.test.ts`

**Interfaces:**
- Produces: `ObjectiveInputSchema`, `PlanOutputSchema`, `CompetitorListSchema`, `ApiSuccess`/`ApiError` helpers

- [ ] **Step 1: 失败测试**

```typescript
import { describe, it, expect } from "vitest";
import { ObjectiveInputSchema } from "./objective";

describe("ObjectiveInputSchema", () => {
  it("rejects title shorter than 2", () => {
    const r = ObjectiveInputSchema.safeParse({
      title: "a",
      analysisTarget: "飞书",
      useCase: "老板汇报",
      audience: "直属领导",
      markets: ["中国大陆"],
      deadline: "一周内",
      outputFormats: ["PDF"],
    });
    expect(r.success).toBe(false);
  });
});
```

- [ ] **Step 2: 实现 schema 并让测试通过**

`ObjectiveInputSchema` 字段对齐 PRD：`title`(2–50)、`analysisTarget`、`useCase`、`audience`、`markets`、`deadline`、`outputFormats`、`knownCompetitors?`、`notes?`。

节点输出最小集：
```typescript
export const PlanOutputSchema = z.object({
  goal: z.string(),
  deliverables: z.array(z.string()).min(1),
  steps: z.array(z.object({
    code: z.string(),
    title: z.string(),
    aiResponsibility: z.string(),
    userResponsibility: z.string(),
  })).min(6),
  estimatedMinutes: z.number().int().positive(),
});

export const CompetitorListSchema = z.object({
  competitors: z.array(z.object({
    name: z.string().min(1),
    website: z.string().url().optional(),
    positioning: z.string(),
    recommendationReason: z.string(),
    matchScore: z.number().min(0).max(100),
    confidence: z.enum(["HIGH", "MEDIUM", "LOW"]),
  })).min(3).max(10),
  uncertainties: z.array(z.string()),
});
```

- [ ] **Step 3: Commit**

```bash
git add packages/schemas
git commit -m "feat(schemas): add objective and node output zod schemas"
```

---

### Task 5: `project-engine` 模板与步进规则（TDD）

**Files:**
- Create: `packages/project-engine/src/templates/competitor-analysis/v1.ts`
- Create: `packages/project-engine/src/engine.ts`
- Test: `packages/project-engine/src/engine.test.ts`

**Interfaces:**
- Consumes: `NodeCode`, `ProjectStatus`, `canTransitionProject`
- Produces:
```typescript
export type TemplateStepDef = {
  code: NodeCode;
  title: string;
  sequence: number;
  requiresUserInput: boolean;
  requiresUserDecision: boolean;
  requiresPayment: boolean;
  uiStep: 1 | 2 | 3 | 4 | 5 | 6;
};

export function getTemplateV1(): { code: "competitor-analysis"; version: 1; steps: TemplateStepDef[] };

export function assertCanStartNode(args: {
  projectStatus: ProjectStatus;
  previousStepStatus: StepStatus | null;
  node: TemplateStepDef;
  hasPaidEntitlement: boolean;
}): void; // throws Error with code STEP_INVALID_TRANSITION | ENTITLEMENT_REQUIRED
```

- [ ] **Step 1: 写测试**

```typescript
import { describe, it, expect } from "vitest";
import { assertCanStartNode, getTemplateV1 } from "./engine";

describe("assertCanStartNode", () => {
  const report = getTemplateV1().steps.find((s) => s.code === "GENERATE_REPORT")!;

  it("blocks GENERATE_REPORT without entitlement", () => {
    expect(() =>
      assertCanStartNode({
        projectStatus: "ACTIVE",
        previousStepStatus: "SUCCEEDED",
        node: report,
        hasPaidEntitlement: false,
      }),
    ).toThrow(/ENTITLEMENT_REQUIRED/);
  });

  it("allows GENERATE_REPORT with entitlement when previous succeeded", () => {
    expect(() =>
      assertCanStartNode({
        projectStatus: "ACTIVE",
        previousStepStatus: "SUCCEEDED",
        node: report,
        hasPaidEntitlement: true,
      }),
    ).not.toThrow();
  });
});
```

- [ ] **Step 2: 实现 `getTemplateV1` + `assertCanStartNode`**

规则：
- `previousStepStatus` 非 null 且不是 `SUCCEEDED` → 抛 `STEP_INVALID_TRANSITION`（首节点除外）
- `node.requiresPayment && !hasPaidEntitlement` → `ENTITLEMENT_REQUIRED`
- `projectStatus === "CANCELLED"` → 不可 start

- [ ] **Step 3: 测试通过并 commit**

```bash
pnpm --filter @stepdone/project-engine test
git add packages/project-engine
git commit -m "feat(project-engine): competitor-analysis v1 gates"
```

---

### Task 6: `packages/config` + `packages/auth` mock session

**Files:**
- Create: `packages/config/src/env.ts`
- Create: `packages/auth/src/session.ts`
- Create: `packages/auth/src/mock-login.ts`
- Test: `packages/auth/src/session.test.ts`

先实现 `loadEnv()`（zod 校验 `DATABASE_URL`/`REDIS_URL`/`SESSION_SECRET`/`MOCK_PAYMENTS`/`APP_ENV`/`APP_URL`），auth 与 worker 共用。

**Interfaces:**
- Produces:
```typescript
export type SessionPayload = { userPublicId: string; sessionPublicId: string; exp: number };
export function signSession(payload: SessionPayload, secret: string): string;
export function verifySession(token: string, secret: string): SessionPayload | null;
export async function mockLogin(prisma: PrismaClient, displayName?: string): Promise<{
  userPublicId: string;
  sessionToken: string;
}>;
```

- [ ] **Step 1–4: TDD 实现 HMAC-SHA256 签名 token（`base64url(payload).base64url(sig)`），`mockLogin` 创建 User + UserIdentity(provider=`MOCK`) + UserSession(tokenHash)

Cookie 名：`stepdone_session`；HttpOnly；Path=/；SameSite=Lax；生产 Secure。

```bash
pnpm --filter @stepdone/auth test
git commit -m "feat(auth): mock login and signed session tokens"
```

---

### Task 7: `packages/payments` mock 确认（TDD）

**Files:**
- Create: `packages/payments/src/create-order.ts`
- Create: `packages/payments/src/mock-confirm.ts`
- Test: `packages/payments/src/mock-confirm.test.ts`

**Interfaces:**
- Produces:
```typescript
export async function createOrder(input: {
  userId: bigint;
  projectId: bigint;
  productCode: "STANDARD_PROJECT" | "PRO_PROJECT";
}): Promise<{ orderPublicId: string; amountFen: number }>;

export async function mockConfirmPayment(input: {
  orderPublicId: string;
  allowMock: boolean;
}): Promise<{ orderStatus: "PAID" }>;
```

- [ ] **Step 1: 测试重复确认幂等**

```typescript
it("is idempotent on duplicate confirm", async () => {
  // seed user/project/order PENDING
  await mockConfirmPayment({ orderPublicId, allowMock: true });
  await mockConfirmPayment({ orderPublicId, allowMock: true });
  const entitlements = await prisma.entitlement.count({ where: { userId } });
  expect(entitlements).toBe(1);
});

it("rejects when allowMock is false", async () => {
  await expect(
    mockConfirmPayment({ orderPublicId, allowMock: false }),
  ).rejects.toThrow(/PAYMENT_NOT_ALLOWED/);
});
```

- [ ] **Step 2: 实现事务**

`mockConfirmPayment` 在交互事务中：
1. 锁定 order；若已 `PAID` 直接返回
2. 写 `payment_transactions`
3. order → `PAID`
4. upsert/发放 entitlement（STANDARD/PRO credit + REPORT_EXPORT；PRO 另加 PPT_EXPORT、RESEARCH_RETRY、REPORT_REGENERATE）
5. 写 `entitlement_transactions`
6. 项目 `PAYMENT_REQUIRED` → `ACTIVE`（经 `canTransitionProject`）
7. 写 `outbox_events` topic=`agent-default` payload=`{ type:"START_NODE", projectId, nodeCode:"GENERATE_REPORT" }`
8. 写 `project_events`

金额只从 `products.priceFen` 读取。

- [ ] **Step 3: Commit**

```bash
git add packages/payments
git commit -m "feat(payments): create order and idempotent mock confirm"
```

---

### Task 8: `agent-core` 幂等与租约

**Files:**
- Create: `packages/agent-core/src/idempotency.ts`
- Create: `packages/agent-core/src/lease.ts`
- Create: `packages/agent-core/src/types.ts`
- Test: `packages/agent-core/src/idempotency.test.ts`

**Interfaces:**
```typescript
export function buildIdempotencyKey(projectId: string, stepRunId: string, nodeCode: string, inputVersion: number): string;
// `${projectId}:${stepRunId}:${nodeCode}:${inputVersion}`

export type NodeResult<T> =
  | { type: "completed"; output: T }
  | { type: "waiting_user"; prompt: { question: string; options?: string[] }; partialOutput?: Partial<T> }
  | { type: "retry"; reason: string; retryAfterMs?: number }
  | { type: "failed"; code: string; message: string };

export async function acquireLease(prisma, agentRunId: bigint, workerId: string, ttlMs: 60_000): Promise<boolean>;
export async function heartbeatLease(prisma, agentRunId: bigint, workerId: string): Promise<void>;
```

- [ ] **Step 1–4: TDD 实现后 commit**

```bash
git commit -m "feat(agent-core): idempotency keys and run leases"
```

---

### Task 9: Web API — auth + projects CRUD + revision

**Files:**
- Create: `apps/web/app/api/auth/mock-login/route.ts`
- Create: `apps/web/app/api/auth/logout/route.ts`
- Create: `apps/web/app/api/auth/me/route.ts`
- Create: `apps/web/app/api/projects/route.ts`
- Create: `apps/web/app/api/projects/[projectId]/route.ts`
- Create: `apps/web/lib/api.ts`（`jsonOk`/`jsonErr`/`requireUser`）
- Create: `apps/web/lib/session.ts`
- Test: `packages/database` 或 `apps/web` 集成测试 `apps/web/app/api/projects/projects.revision.test.ts`（用 vitest + prisma）

**Interfaces:**
- `POST /api/auth/mock-login` → Set-Cookie + `{ user }`
- `POST /api/projects` body: ObjectiveInput → 创建 DRAFT/ACTIVE 项目，锁定 templateVersion，写 DEFINE_OBJECTIVE 完成，入队 CREATE_PLAN（outbox）
- `PATCH /api/projects/:projectId` body: `{ revision, changes }` → 乐观锁

实现 `PATCH`：
```typescript
const updated = await prisma.project.updateMany({
  where: { id: project.id, revision: body.revision },
  data: { ...mappedChanges, revision: { increment: 1 } },
});
if (updated.count === 0) return jsonErr("PROJECT_REVISION_CONFLICT", 409);
```

改 API 前阅读：`node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md`（或同目录 route 文档）。

- [ ] **Step 1: 手测/自动化**

```bash
pnpm --filter @stepdone/web dev
# curl mock-login, create project, patch with stale revision expect 409
```

- [ ] **Step 2: Commit**

```bash
git commit -m "feat(web): mock auth and project CRUD with revision lock"
```

---

### Task 10: Web API — steps decision/start + sources/artifacts 读接口

**Files:**
- Create: `apps/web/app/api/projects/[projectId]/steps/route.ts`
- Create: `apps/web/app/api/projects/[projectId]/steps/[stepCode]/start/route.ts`
- Create: `apps/web/app/api/projects/[projectId]/steps/[stepCode]/decision/route.ts`
- Create: `apps/web/app/api/projects/[projectId]/steps/[stepCode]/input/route.ts`
- Create: `apps/web/app/api/projects/[projectId]/steps/[stepCode]/retry/route.ts`
- Create: `apps/web/app/api/projects/[projectId]/sources/route.ts`
- Create: `apps/web/app/api/projects/[projectId]/artifacts/route.ts`
- Create: `apps/web/app/api/projects/[projectId]/status/route.ts`

**Interfaces:**
- `decision`：写入 `project_decisions`，推进下一步（或入队下一 AI 节点）；**绝不**让 Worker 代写决策
- `start`：调用 `assertCanStartNode`，创建 `project_step_runs` + `agent_runs` + `outbox_events` 同事务

- [ ] **Step 1: 实现并写集成测试「未付费 start GENERATE_REPORT 失败」**

- [ ] **Step 2: Commit**

```bash
git commit -m "feat(web): step start/decision APIs with engine gates"
```

---

### Task 11: Agent worker — Outbox + BullMQ + fixtures

**Files:**
- Create: `apps/agent-worker/package.json`
- Create: `apps/agent-worker/src/index.ts`
- Create: `apps/agent-worker/src/outbox.ts`
- Create: `apps/agent-worker/src/queues.ts`
- Create: `apps/agent-worker/src/fixtures/index.ts`
- Create: `apps/agent-worker/src/runner.ts`
- Create: `apps/agent-worker/src/health.ts`

**Interfaces:**
- 健康检查：进程内 `http://127.0.0.1:4101/health/live` 与 `/health/ready`（ready 检查 MySQL+Redis）
- Outbox：每 1s `SELECT ... FOR UPDATE SKIP LOCKED` 投递到 `agent-default` / `agent-heavy` / `export`
- Runner：acquireLease → `fixtures[nodeCode](ctx)` → Zod validate → 保存输出到 `project_step_runs.output` / sources / artifacts → 更新状态 → `project_events` → Redis publish `project:{publicId}`
- Fixture 延迟：默认 1000ms；`RESEARCH_SOURCES` 5000ms
- `requiresUserDecision` 的 AI 准备节点完成后将项目置 `WAITING_USER`，**不**自动 decision

`queues.ts` 消息体：
```typescript
type AgentJob = {
  agentRunId: string; // publicId
  projectId: string;
  stepRunId: string;
  nodeCode: NodeCode;
  schemaVersion: 1;
};
```

- [ ] **Step 1: 实现 worker 并本地跑通 CREATE_PLAN**

```bash
pnpm --filter @stepdone/agent-worker dev
# 创建项目后等待 plan 输出出现在 DB
```

- [ ] **Step 2: Commit**

```bash
git commit -m "feat(agent-worker): outbox dispatcher, bullmq consumer, fixtures"
```

---

### Task 12: SSE events API

**Files:**
- Create: `apps/web/app/api/projects/[projectId]/events/route.ts`
- Create: `apps/web/lib/redis.ts`
- Create: `packages/database` 中确保 `ProjectEvent` 写入含 `type/stage/message/percent`

**Interfaces:**
- `GET /api/projects/:projectId/events` → `text/event-stream`
- 鉴权 + 所有权校验
- 订阅 Redis channel `project:{publicId}`；每 15s 心跳注释
- 客户端可用 `Last-Event-ID` 做瘦身补拉（可选：重连时用 `/status`）

```typescript
export async function GET(req: Request, ctx: { params: Promise<{ projectId: string }> }) {
  // read next docs for route handlers + streaming
  const stream = new ReadableStream({
    start(controller) {
      const enc = new TextEncoder();
      const send = (data: object, id?: string) => {
        if (id) controller.enqueue(enc.encode(`id: ${id}\n`));
        controller.enqueue(enc.encode(`data: ${JSON.stringify(data)}\n\n`));
      };
      // subscribe + heartbeat
    },
  });
  return new Response(stream, {
    headers: {
      "Content-Type": "text/event-stream",
      "Cache-Control": "no-cache",
      Connection: "keep-alive",
    },
  });
}
```

- [ ] **Step 1: 手测 SSE 能收到 PROGRESS**

- [ ] **Step 2: Commit**

```bash
git commit -m "feat(web): project SSE event stream with redis pubsub"
```

---

### Task 13: Orders API + 主链路 API 验收脚本

**Files:**
- Create: `apps/web/app/api/orders/route.ts`
- Create: `apps/web/app/api/orders/[orderId]/route.ts`
- Create: `apps/web/app/api/payments/mock/confirm/route.ts`
- Create: `apps/web/app/api/entitlements/route.ts`
- Create: `scripts/smoke-main-path.mjs`
- Test: `packages/payments` 已有单测；增加 `scripts` 文档到 README 片段

**Interfaces:**
- `POST /api/orders` `{ projectId, productCode }`
- `POST /api/payments/mock/confirm` `{ orderId }` 读取 `process.env.MOCK_PAYMENTS === "1"`
- Smoke 脚本步骤：login → create project → wait plan → decisions through preview → order → mock confirm → assert artifact report exists or GENERATE_REPORT queued

```javascript
// scripts/smoke-main-path.mjs — use fetch + cookie jar; poll /api/projects/:id/status until conditions
```

- [ ] **Step 1: 跑通 smoke（可先推进到支付成功 + entitlement，报告由 worker fixture 生成）**

```bash
pnpm docker:up
pnpm db:migrate && pnpm db:seed
pnpm dev
node scripts/smoke-main-path.mjs
```
Expected: exit 0；打印 `PAYMENT_OK` 与 `REPORT_NODE_OK`

- [ ] **Step 2: Commit**

```bash
git commit -m "feat(web): orders mock payment APIs and smoke script"
```

---

### Task 14: Slice A 收尾文档

**Files:**
- Modify: `README.md`（本地启动：docker、pnpm、seed、dev、smoke）
- Modify: spec 状态不必改；在 plan 顶部可手勾 Task 完成情况

- [ ] **Step 1: 更新 README 最小可用说明（中文）**

- [ ] **Step 2: Commit**

```bash
git commit -m "docs: document slice A local smoke path"
```

---

## Slice A 完成标准

- [ ] `pnpm --filter @stepdone/domain test` 等包单测绿
- [ ] `node scripts/smoke-main-path.mjs` 绿
- [ ] 未付费无法 start `GENERATE_REPORT`
- [ ] 重复 mock confirm 不双发权益
- [ ] 关 worker 再开，Outbox/队列可继续（租约过期可接管）

完成后执行 **Slice B** 计划接 UI 与 Playwright。
